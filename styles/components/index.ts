// * Import styles
import contractStyles from "./ContractList.module.css";
import footerStyles from "./Footer.module.css";
import headerStyles from "./Header.module.css";

// * Redefine expoert vards
const grid = contractStyles.grid;
const card = contractStyles.card;
const row = contractStyles.row;
const column = contractStyles.column;
const footer = footerStyles.footer;
const logo = footerStyles.logo;
const headerContainer = headerStyles.headerContainer;
const searchBar = headerStyles.searchBar;

// * EXPORT!!
export { grid, card, row, column, footer, logo, headerContainer, searchBar };
